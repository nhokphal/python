# define "def it means define" function in py

def square(x):
    return x * x # multiply by itself


for i in range(10):
    print(f"the square of {i} is {square(i)}") # loop 10 times, each time we loop we print something else in the square function above
